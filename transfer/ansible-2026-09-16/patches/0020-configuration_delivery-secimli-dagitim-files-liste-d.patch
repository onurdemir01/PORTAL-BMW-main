From 22a9231cf4501cf3e43ab3a20f016f5ae0fd7d9f Mon Sep 17 00:00:00 2001
From: Onur Demir <demir-onur@hotmail.com>
Date: Wed, 16 Sep 2026 10:37:54 +0300
Subject: [PATCH 20/22] configuration_delivery: secimli dagitim - files
 (liste/dizge), target_hosts, dry_run (fark), yedek + nginx -t + geri alma,
 reload yalniz degisiklikte

- hangi dosyanin gidecegi playbook'ta satir yorumlayarak degil  ile secilir
  (Portal: coklu secim onay kutulari); nginx.conf kaynagi surume gore otomatik
- kabuk bloklari files/ altinda (fix_permissions.sh: dhparam yalniz yoksa; nginx_test_or_reload.sh)
- README (Portal kurulumu), tests/check_configuration_delivery.py

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
---
 bmw_nginx/configuration_delivery/README.md    |  55 ++++
 .../configuration_delivery.yaml               | 241 +++++++++++-------
 .../files/fix_permissions.sh                  |  18 ++
 .../files/nginx_test_or_reload.sh             |  20 ++
 .../tests/check_configuration_delivery.py     |  62 +++++
 5 files changed, 299 insertions(+), 97 deletions(-)
 create mode 100644 bmw_nginx/configuration_delivery/README.md
 create mode 100644 bmw_nginx/configuration_delivery/files/fix_permissions.sh
 create mode 100644 bmw_nginx/configuration_delivery/files/nginx_test_or_reload.sh
 create mode 100644 bmw_nginx/tests/check_configuration_delivery.py

diff --git a/bmw_nginx/configuration_delivery/README.md b/bmw_nginx/configuration_delivery/README.md
new file mode 100644
index 000000000..01dc33a27
--- /dev/null
+++ b/bmw_nginx/configuration_delivery/README.md
@@ -0,0 +1,55 @@
+# Configuration Delivery (`configuration_delivery`)
+
+nginx kurulum dosyalarını (`bmw_defaults.conf`, `proxy_settings.conf`, `rate_limits.conf`,
+`log_format.conf`, `gt-error-page.html`, `nginx.conf`) seçilen sunuculara **seçimli** dağıtır.
+Kaynak `files/` altındaki kopyalardır; `nginx_installation` referansıyla aynı tutulmalıdır
+(Denetim > Nginx Audit "Dosya farkı" bunu ölçer).
+
+| | |
+|---|---|
+| Playbook | `bmw_nginx/configuration_delivery/configuration_delivery.yaml` |
+| Kabuk | `files/fix_permissions.sh`, `files/nginx_test_or_reload.sh` |
+| Portal | Self Servis > Otomasyon > *Nginx - Configuration Delivery* (aşağıda kurulum) |
+
+## 1. Girdiler
+
+| Değişken | Tip | Açıklama |
+|---|---|---|
+| `files` | liste **veya** virgül/satır ayrımlı dizge | Üzerine yazılacak dosyalar. Geçerli adlar: `bmw_defaults.conf`, `proxy_settings.conf`, `rate_limits.conf`, `log_format.conf`, `gt-error-page.html`, `nginx.conf` |
+| `target_hosts` | liste veya virgüllü | Hedef sunucular (`hosts:` buradan; yoksa `all` + AWX limit) |
+| `dry_run` | bool, varsayılan `false` | `true`: yalnız **fark** gösterir (check mode + diff), hiçbir şey yazmaz, reload yok |
+| `fix_permissions` | bool, varsayılan `true` | sahiplik/izin düzeni; `dhparam.pem` **yoksa** üretilir (eskiden her koşuda yeniden üretiliyordu) |
+| `version` | `Plus` / `Opensource`, opsiyonel | `nginx.conf` kaynağı (`nginx_plus.conf` / `nginx_opensource.conf`). Verilmezse sunucudan `nginx -v` ile **otomatik** |
+
+Eskiden hangi dosyanın gideceği playbook'ta satır yorumlayarak seçiliyordu; artık `files` ile.
+
+## 2. Akış
+
+1. `files` normalize edilir, bilinmeyen ad varsa iş **başlamadan** durur.
+2. Seçilen dosyalar `backup: yes` ile kopyalanır (`dry_run` ise yalnız fark).
+3. `fix_permissions` → `files/fix_permissions.sh`.
+4. `nginx -t` (plus/OSS ayrımı `files/nginx_test_or_reload.sh`); düşerse **tüm** dosyalar
+   yedekten geri alınır, host FAIL. Geçerse **yalnız bir şey değiştiyse** reload.
+
+## 3. Portal'da kurulum (bir kez, Admin)
+
+Self Servis'te bu template için **Alan Ayarları**: AWX survey kapalıysa Survey Tasarımcısı:
+
+| Alan | Tip | Ayar |
+|---|---|---|
+| `files` | **Çoklu Seçim (onay kutuları)** | seçenekler: yukarıdaki 6 dosya adı; zorunlu |
+| `target_hosts` | Çoklu Seçim | Seçenek kaynağı: **Nginx sunucuları (Nginx Envanteri)**; zorunlu |
+| `dry_run` | Tekli Seçim `true`/`false` | varsayılan `false` |
+| `fix_permissions` | Tekli Seçim | varsayılan `true` (ya da gizli) |
+
+Portal çoklu seçimi AWX'e **liste** olarak gönderir; playbook liste ve dizgeyi aynı işler.
+Kullanıcı dosyaları ve sunucuları tikleyerek seçer; önce `dry_run=true` ile farkı görmesi
+önerilir.
+
+## 4. Elle (AWX) kullanım
+
+```
+-e '{"files": ["bmw_defaults.conf","log_format.conf"], "target_hosts": "GBNGXT33,GBNGXT34", "dry_run": true}'
+```
+
+Test: `python bmw_nginx/tests/check_configuration_delivery.py`
diff --git a/bmw_nginx/configuration_delivery/configuration_delivery.yaml b/bmw_nginx/configuration_delivery/configuration_delivery.yaml
index bf9b78da9..439d5e657 100644
--- a/bmw_nginx/configuration_delivery/configuration_delivery.yaml
+++ b/bmw_nginx/configuration_delivery/configuration_delivery.yaml
@@ -1,110 +1,157 @@
-- name: --- [Play] Configuration Delivery
-  hosts: all
+---
+# configuration_delivery.yaml — nginx kurulum dosyalarini SECIMLI dagitir (2026-09-16).
+#
+# ONCEKI HAL: hangi dosyanin gidecegi playbook'ta satir yorumlayarak seciliyordu ve her
+# kosuda dhparam yeniden uretiliyordu. SIMDI: Portal (Self Servis) ya da AWX'ten
+#   files        : uzerine yazilacak dosyalar (liste ya da virgul/satir ayrimli dizge)
+#                  bmw_defaults.conf | proxy_settings.conf | rate_limits.conf |
+#                  log_format.conf | gt-error-page.html | nginx.conf
+#   target_hosts : hedef sunucular (liste ya da virgullu; Portal Nginx Envanteri'nden secilir)
+#   dry_run      : true -> yalniz FARK gosterir, hicbir sey yazmaz (varsayilan false)
+#   fix_permissions : true (varsayilan) -> sahiplik/izinler duzeltilir, dhparam YOKSA uretilir
+# nginx.conf icin plus/opensource AYRIMI sunucudan (nginx -v) otomatik; `version` extra var
+# verilirse (Plus|Opensource) o kazanir (eski davranisla uyum).
+# Her yazilan dosyanin yedegi alinir; nginx -t duserse TUM dosyalar geri alinir ve host FAIL;
+# gecerse reload. Kabuk bloklari files/ altinda (fix_permissions.sh, nginx_test_or_reload.sh).
+# Ayrinti: README.md
+
+- name: "--- [Play] Configuration Delivery (secimli) ---"
+  hosts: "{{ target_hosts | default('all') }}"
   gather_facts: no
+  vars:
+    files: []
+    dry_run: false
+    fix_permissions: true
+    # dosya -> hedef yol (nginx.conf kaynagi surume gore secilir)
+    cd_targets:
+      bmw_defaults.conf: /usr/nginx/conf/bmw_defaults.conf
+      proxy_settings.conf: /usr/nginx/conf/proxy_settings.conf
+      rate_limits.conf: /usr/nginx/conf/rate_limits.conf
+      log_format.conf: /usr/nginx/conf/log_format.conf
+      gt-error-page.html: /usr/nginx/html/gt-error-page.html
+      nginx.conf: /usr/nginx/nginx.conf
   tasks:
-    - name: --- [Task] Copy log_format.conf configuration file ---
+    # Portal coklu secimi LISTE, AWX/elle kullanim virgul ya da satir sonu ayrimli DIZGE
+    # gonderebilir; ikisi de tek listeye indirgenir.
+    - name: --- [Task] Secilen dosyalari normalize et ---
+      ansible.builtin.set_fact:
+        cd_files: >-
+          {{ (files if (files is not string and files is iterable) else (files | string | replace('\n', ',') | split(',')))
+             | map('trim') | reject('equalto', '') | unique | list }}
+
+    - name: --- [Task] En az bir dosya secilmis ve hepsi taninan dosyalar olmali ---
+      ansible.builtin.assert:
+        that:
+          - (cd_files | length) > 0
+          - (cd_files | difference(cd_targets.keys() | list) | length) == 0
+        fail_msg: >-
+          files bos ya da bilinmeyen dosya iceriyor: {{ cd_files | difference(cd_targets.keys() | list) | join(', ') }}.
+          Gecerli: {{ cd_targets.keys() | join(', ') }}
+
+    - name: --- [Task] nginx surumu (plus / opensource) ---
       become: true
+      become_method: dzdo
       become_user: www
+      ansible.builtin.shell: dzdo /usr/nginx/sbin/nginx -v 2>&1 | awk -F "/" '{print $2}'
+      register: cd_ver
+      changed_when: false
+      failed_when: false
+      when: "'nginx.conf' in cd_files"
+
+    - name: --- [Task] nginx.conf kaynagini sec ---
+      ansible.builtin.set_fact:
+        cd_nginx_src: >-
+          {{ 'nginx_plus.conf'
+             if ((version | default('') | lower) == 'plus'
+                 or ((version | default('') | length) == 0 and 'plus' in (cd_ver.stdout | default('') | lower)))
+             else 'nginx_opensource.conf' }}
+      when: "'nginx.conf' in cd_files"
+
+    - name: --- [Task] Plan ---
+      ansible.builtin.debug:
+        msg: >-
+          {{ inventory_hostname }} · {{ 'DENEME (dry_run: yazilmaz)' if (dry_run | bool) else 'UYGULA' }} ·
+          {{ cd_files | join(', ') }}{{ (' · nginx.conf <- ' ~ cd_nginx_src) if ('nginx.conf' in cd_files) else '' }}
+
+    # backup: yes -> her item icin backup_file (rollback icin). check_mode + diff -> dry_run'da
+    # yalniz fark gosterilir. Kaynak dosya adi hedef adindan farkli olabilir (nginx.conf).
+    - name: "--- [Task] {{ 'FARK' if (dry_run | bool) else 'Kopyala' }}: secilen dosyalar (yedekli) ---"
+      become: true
       become_method: dzdo
+      become_user: www
       ansible.builtin.copy:
-        src: "{{ item }}"
-        dest: "/usr/nginx/conf/{{ item }}"
+        src: "{{ cd_nginx_src if item == 'nginx.conf' else item }}"
+        dest: "{{ cd_targets[item] }}"
         owner: www
         mode: '0740'
-      with_items:
-      - log_format.conf
+        backup: yes
+      loop: "{{ cd_files }}"
+      register: cd_copy
+      check_mode: "{{ dry_run | bool }}"
+      diff: yes
 
-#    - name: --- [Task] Copy rate_limits.conf configuration file ---
-#      become: true
-#      become_user: www
-#      become_method: dzdo
-#      ansible.builtin.copy:
-#        src: "{{ item }}"
-#        dest: "/usr/nginx/conf/{{ item }}"
-#        owner: www
-#        mode: '0740'
-#      with_items:
-#      - rate_limits.conf
-#
-#    - name: --- [Task] Copy bmw_defaults.conf configuration file ---
-#      become: true
-#      become_user: www
-#      become_method: dzdo
-#      ansible.builtin.copy:
-#        src: "{{ item }}"
-#        dest: "/usr/nginx/conf/{{ item }}"
-#        owner: www
-#        mode: '0740'
-#      with_items:
-#      - bmw_defaults.conf
-#
-#    - name: --- [Task] Copy gt-error-page.html file ---
-#      become: true
-#      become_user: www
-#      become_method: dzdo
-#      ansible.builtin.copy:
-#        src: "{{ item }}"
-#        dest: "/usr/nginx/html/{{ item }}"
-#        owner: www
-#        mode: '0740'
-#      with_items:
-#      - gt-error-page.html
-#
-#    - name: --- [Task] Copy nginx.conf configuration file ---
-#      become: true
-#      become_user: www
-#      become_method: dzdo
-#      ansible.builtin.copy:
-#        src: "{{ item }}"
-#        dest: "/usr/nginx/nginx.conf"
-#        owner: www
-#        mode: '0740'
-#      with_items:
-#      - nginx_plus.conf
-#
-#    - name: --- [Task] Copy nginx.conf configuration file ---
-#      become: true
-#      become_user: www
-#      become_method: dzdo
-#      ansible.builtin.copy:
-#        src: "{{ item }}"
-#        dest: "/usr/nginx/nginx.conf"
-#        owner: www
-#        mode: '0740'
-#      with_items:
-#      - nginx_opensource.conf
-#      when: version is match('Opensource')
-#
-#    - name: --- [Task] Delete X-Frame-Options Header ---
-#      become: true
-#      become_user: www
-#      become_method: dzdo
-#      ansible.builtin.shell: |
-#        sed -i "/add_header X-Frame-Options 'SAMEORIGIN' always;/d" /usr/nginx/conf/bmw_defaults.conf
-#      when: xframeoptions is match('false')
-#
-#    - name: --- [Task] Delete Referrer-Policy Header ---
-#      become: true
-#      become_user: www
-#      become_method: dzdo
-#      ansible.builtin.shell: |
-#        sed -i "/add_header Referrer-Policy 'no-referrer';/d" /usr/nginx/conf/bmw_defaults.conf
-#      when: referrer is match('false')
+    - name: "--- [Task] Deneme modu: degisecek dosyalar ---"
+      ansible.builtin.debug:
+        msg: >-
+          {{ inventory_hostname }}: degisecek {{ cd_copy.results | selectattr('changed') | map(attribute='item') | list | join(', ') | default('hicbiri', true) }}
+          · ayni {{ cd_copy.results | rejectattr('changed') | map(attribute='item') | list | join(', ') | default('-', true) }}
+      when: dry_run | bool
+
+    - name: --- [Task] Deneme modunda burada biter ---
+      ansible.builtin.meta: end_host
+      when: dry_run | bool
 
-    - name: --- [Task] Reload Nginx ---
+    - name: --- [Task] Izinler / sahiplik / dhparam (yoksa) ---
       become: true
+      become_method: dzdo
       become_user: www
+      ansible.builtin.script:
+        cmd: files/fix_permissions.sh
+      when: fix_permissions | bool
+      changed_when: false
+
+    - name: --- [Task] nginx -t ---
+      become: true
       become_method: dzdo
-      ansible.builtin.shell: |
-        nginx_version=$(dzdo /usr/nginx/sbin/nginx -v 2>&1 | awk -F "/" '{print $2}')
-        if [[ "$nginx_version" == *plus* ]]; then
-          dzdo /usr/nginx/sbin/nginx -p /usr/nginx/ -c /usr/nginx/nginx.conf -e /web_log/error.log -s reload
-        else
-          dzdo /usr/nginx/sbin/nginx -s reload
-        fi
-        exit_code=$(echo $?)
-        if [[ $exit_code -eq 0 ]];then
-          exit 0
-        else
-          exit 44
-        fi
\ No newline at end of file
+      become_user: www
+      ansible.builtin.script:
+        cmd: files/nginx_test_or_reload.sh test
+      register: cd_test
+      failed_when: false
+      changed_when: false
+
+    - name: "--- [Task] nginx -t DUSTU -> yedekten geri al ---"
+      become: true
+      become_method: dzdo
+      become_user: www
+      ansible.builtin.copy:
+        src: "{{ item.backup_file }}"
+        dest: "{{ item.dest }}"
+        remote_src: yes
+        owner: www
+        mode: '0740'
+      loop: "{{ cd_copy.results | selectattr('backup_file', 'defined') | list }}"
+      loop_control:
+        label: "{{ item.dest }}"
+      when: cd_test.rc != 0
+
+    - name: --- [Task] Geri alindi, host FAIL ---
+      ansible.builtin.fail:
+        msg: "{{ inventory_hostname }}: nginx -t dustu, dosyalar yedekten geri alindi: {{ ((cd_test.stdout_lines | default([])) + (cd_test.stderr_lines | default([]))) | join(' / ') | truncate(600) }}"
+      when: cd_test.rc != 0
+
+    - name: --- [Task] nginx reload ---
+      become: true
+      become_method: dzdo
+      become_user: www
+      ansible.builtin.script:
+        cmd: files/nginx_test_or_reload.sh reload
+      when: cd_copy.results | selectattr('changed') | list | length > 0
+      changed_when: true
+
+    - name: --- [Task] Sonuc ---
+      ansible.builtin.debug:
+        msg: >-
+          {{ inventory_hostname }}: {{ cd_copy.results | selectattr('changed') | list | length }} dosya degisti
+          ({{ cd_copy.results | selectattr('changed') | map(attribute='item') | list | join(', ') | default('-', true) }}),
+          nginx -t gecti{{ ', reload yapildi' if (cd_copy.results | selectattr('changed') | list | length) > 0 else ', degisiklik yok - reload yapilmadi' }}
diff --git a/bmw_nginx/configuration_delivery/files/fix_permissions.sh b/bmw_nginx/configuration_delivery/files/fix_permissions.sh
new file mode 100644
index 000000000..a414cae33
--- /dev/null
+++ b/bmw_nginx/configuration_delivery/files/fix_permissions.sh
@@ -0,0 +1,18 @@
+#!/bin/bash
+# fix_permissions.sh - configuration_delivery: sahiplik/izin duzeni + dhparam (YOKSA).
+# Onceden playbook'a gomulu shell blogunda her kosuda `openssl dhparam 2048` yeniden
+# uretiliyordu (yavas, gereksiz); simdi yalniz dosya yoksa uretilir.
+set -u
+[ -f /usr/nginx/conf/custom.conf ] || touch /usr/nginx/conf/custom.conf
+dzdo chown -R www /usr/nginx/
+chmod 740 -R /usr/nginx/*
+if [ ! -s /usr/nginx/ssl/dhparam.pem ]; then
+  mkdir -p /usr/nginx/ssl
+  openssl dhparam -out /usr/nginx/ssl/dhparam.pem 2048
+fi
+find /usr/nginx -type d -exec chmod 750 {} \;
+find /usr/nginx -type f -exec chmod 640 {} \;
+chmod 400 /usr/nginx/ssl/*
+chmod 755 /usr/nginx/
+chmod 740 /usr/nginx/sbin/nginx
+exit 0
diff --git a/bmw_nginx/configuration_delivery/files/nginx_test_or_reload.sh b/bmw_nginx/configuration_delivery/files/nginx_test_or_reload.sh
new file mode 100644
index 000000000..1dc97a66d
--- /dev/null
+++ b/bmw_nginx/configuration_delivery/files/nginx_test_or_reload.sh
@@ -0,0 +1,20 @@
+#!/bin/bash
+# nginx_test_or_reload.sh <test|reload> - nginx (plus ya da OSS) icin -t / -s reload.
+# Diger playbook'lardaki (nginx_ops, api_generator) gomulu blogun files/ altina alinmis hali.
+set -u
+MODE="${1:-test}"
+BIN=/usr/nginx/sbin/nginx
+ver="$(dzdo "$BIN" -v 2>&1 | awk -F "/" '{print $2}')"
+if [ "$MODE" = "reload" ]; then
+  if [[ "$ver" == *plus* ]]; then
+    dzdo "$BIN" -p /usr/nginx/ -c /usr/nginx/nginx.conf -e /web_log/error.log -s reload
+  else
+    dzdo "$BIN" -s reload
+  fi
+else
+  if [[ "$ver" == *plus* ]]; then
+    dzdo "$BIN" -p /usr/nginx/ -c /usr/nginx/nginx.conf -e /web_log/error.log -t
+  else
+    dzdo "$BIN" -t
+  fi
+fi
diff --git a/bmw_nginx/tests/check_configuration_delivery.py b/bmw_nginx/tests/check_configuration_delivery.py
new file mode 100644
index 000000000..0cc43c193
--- /dev/null
+++ b/bmw_nginx/tests/check_configuration_delivery.py
@@ -0,0 +1,62 @@
+#!/usr/bin/env python3
+# -*- coding: utf-8 -*-
+"""configuration_delivery: secimli dosya dagitimi (2026-09-16, Portal'dan tikleyerek secim).
+
+Kilitlenen iddialar (playbook metni + Jinja gercekten kosturulur):
+  1. files liste ya da virgul/satir ayrimli dizge -> tek liste; bos -> assert durdurur
+  2. yalniz taninan 6 dosya; nginx.conf kaynagi surume gore (version > nginx -v)
+  3. copy backup:yes + check_mode dry_run + diff; dry_run'da reload/izin adimi yok
+  4. nginx -t duserse yedekten geri alma + fail; reload yalniz degisiklik varsa
+  5. gomulu shell yok: izinler ve nginx -t/reload files/ altinda; dhparam yalniz yoksa
+  6. hosts target_hosts'tan (Portal coklu secimi), yoksa all
+"""
+import io, os, re, sys
+try:
+    import jinja2
+except ImportError:
+    print("ATLANDI: jinja2 yok"); sys.exit(0)
+
+HERE = os.path.dirname(os.path.abspath(__file__))
+D = os.path.join(os.path.dirname(HERE), "configuration_delivery")
+pb = io.open(os.path.join(D, "configuration_delivery.yaml"), encoding="utf-8").read()
+fixp = io.open(os.path.join(D, "files", "fix_permissions.sh"), encoding="utf-8").read()
+problems = []
+
+# 1) normalize ifadesi
+m = re.search(r"cd_files: >-\n\s+(\{\{[\s\S]*?\}\})", pb)
+if not m:
+    problems.append("1: cd_files set_fact bulunamadi")
+else:
+    e = jinja2.Environment()
+    e.filters['trim'] = lambda s: str(s).strip(); e.filters['unique'] = lambda l: list(dict.fromkeys(l)); e.filters['split'] = lambda s, sep=',': str(s).split(sep)
+    expr = re.sub(r"\s+", " ", m.group(1))
+    t = e.from_string(expr)
+    if t.render(files=["a.conf", "b.conf"]) != "['a.conf', 'b.conf']": problems.append("1: liste girdisi bozuldu")
+    if t.render(files="a.conf, b.conf\nc.conf,,") != "['a.conf', 'b.conf', 'c.conf']": problems.append("1: dizge girdisi (virgul+satir) bolunmedi")
+    if t.render(files="") != "[]": problems.append("1: bos dizge [] olmali")
+if "(cd_files | length) > 0" not in pb: problems.append("1: bos secim assert'i yok")
+# 2
+for f in ("bmw_defaults.conf", "proxy_settings.conf", "rate_limits.conf", "log_format.conf", "gt-error-page.html", "nginx.conf"):
+    if ("%s:" % f) not in pb: problems.append("2: cd_targets'ta yok: " + f)
+if "difference(cd_targets.keys() | list)" not in pb: problems.append("2: bilinmeyen dosya assert'i yok")
+if "'nginx_plus.conf'" not in pb or "'nginx_opensource.conf'" not in pb or "nginx -v" not in pb: problems.append("2: nginx.conf kaynak secimi yok")
+# 3
+if not re.search(r"backup: yes[\s\S]{0,200}check_mode: \"\{\{ dry_run \| bool \}\}\"[\s\S]{0,40}diff: yes", pb): problems.append("3: copy backup/check_mode/diff eksik")
+if "meta: end_host" not in pb or "when: dry_run | bool" not in pb: problems.append("3: dry_run'da erken bitis yok")
+# 4
+if "selectattr('backup_file', 'defined')" not in pb or "remote_src: yes" not in pb: problems.append("4: yedekten geri alma yok")
+if "when: cd_test.rc != 0" not in pb: problems.append("4: -t sonucu rollback'i tetiklemiyor")
+if "nginx_test_or_reload.sh reload" not in pb or "selectattr('changed') | list | length > 0" not in pb: problems.append("4: reload yalniz degisiklikte olmali")
+# 5
+if "shell: |" in pb: problems.append("5: gomulu cok satirli shell var")
+if "files/fix_permissions.sh" not in pb: problems.append("5: izin betigi files/ altindan cagrilmiyor")
+if "if [ ! -s /usr/nginx/ssl/dhparam.pem ]" not in fixp: problems.append("5: dhparam her kosuda yeniden uretiliyor")
+if not os.path.exists(os.path.join(D, "files", "nginx_test_or_reload.sh")): problems.append("5: nginx_test_or_reload.sh yok")
+# 6
+if 'hosts: "{{ target_hosts | default(\'all\') }}"' not in pb: problems.append("6: hosts target_hosts'tan gelmiyor")
+# README
+if "check_configuration_delivery" not in io.open(os.path.join(D, "README.md"), encoding="utf-8").read(): problems.append("README yok/eksik")
+
+if problems:
+    print("HATALAR:"); [print(" -", p) for p in problems]; sys.exit(1)
+print("OK: configuration_delivery secimli dagitim - normalize, taninan dosyalar, dry_run, rollback, files/ betikleri")
-- 
2.55.0.windows.3

